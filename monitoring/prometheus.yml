global:
  scrape_interval: 15s

scrape_configs:
  - job_name: deploybox-api
    metrics_path: /api/v1/metrics
    static_configs:
      # API chạy trên host; container Prometheus gọi ra host qua host.docker.internal
      - targets: ['host.docker.internal:4000']
